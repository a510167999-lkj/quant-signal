# quant-signal-lkj 项目进度

> **更新日期**：2026-08-10  
> **分支**：`codex/main`  
> **最新提交**：`3cf9f22`  
> **当前阶段目标 ID**：`factor-v3-activation-disposable-proof/v1`  
> **本文件用途**：记录研究目标、整体计划、各阶段完成度与下一步，供交接与推进对齐。

---

## 1. 研究目标（冻结契约）

来源：`app/research_goal_contract.py`

| 项 | 约束 |
|----|------|
| 数据源 | **仅 Jiaoch**（Tushare 镜像），正式证据禁止混源 |
| 可交易范围 | 沪深主板 + 创业板：`SSE_MAIN` / `SZSE_MAIN` / `SZSE_CHINEXT` |
| 明确排除 | **北证 BSE**、**科创板 SSE_STAR** |
| 主目标 | 真实成本后 **滚动 12 个月净年化 ≥ 50%** |
| 主目标 | **最大回撤 ≤ 15%** |
| 辅助诊断 | Profit Factor ≥ 1.3、Calmar ≥ 1.5、胜率观察约 52%–60% |
| 生产推荐 | 每日最多 3 条建议；允许 0 条 |
| **禁止** | **自动下单**；未通过 final-OOS 与 50/15 前 **禁止注册生产 profile** |

一句话：在 Jiaoch-only、主板+创业板、无自动交易前提下，做出**普适**（大池 + 简化规则）且可审计的策略证据，而不是脆弱切片“达标”。

---

## 2. 整体计划总览

```
┌─────────────────────────────────────────────────────────────────┐
│ Phase A  平台 MVP（个人信号台 + VPS Web）                         │
│ Phase B  工作流硬化 / 证据门禁 / 生产推荐阻断                      │
│ Phase C  当前大池 PIT 数据层（universe→risk→market→…）          │
│ Phase D  Factor V3 正式训练集物化前置（本文件重点）               │
│ Phase E  正式物化 + 开发矩阵训练 / 评估                           │
│ Phase F  Embargo / Final-OOS + 生产 profile（未开始）             │
│ Phase G  实盘推荐 / 监控 / 仍不自动下单                           │
└─────────────────────────────────────────────────────────────────┘
```

| 阶段 | 主题 | 状态 | 说明 |
|------|------|------|------|
| **A** | 个人信号台 MVP | ✅ 大体完成 | FastAPI + 静态前端、推荐/监控/回测 CLI、VPS 部署骨架 |
| **B** | 证据门禁与生产阻断 | ✅ 运行中 | profile gate 失败 → 0 条推荐；`production_recommendation_eligible` 等三层阻断保持 |
| **C** | 当前大池 PIT 数据 | 🔄 部分完成 | universe/risk 等已有历史产物；与 Factor V3 正式链并行演进 |
| **D** | Factor V3 物化前置 | 🔄 **当前主线** | daily-basic / feature-history / disposable 证明链已齐；正式 TCB 未发 |
| **E** | 正式物化与训练矩阵 | ⏳ 未开始 | 依赖 parent-source / evaluation 真实权威 + formal activation |
| **F** | Embargo / Final-OOS | 🚫 禁止提前打开 | 未达门槛前不消费 embargo/final-OOS |
| **G** | 生产 profile 与实盘推荐 | 🚫 阻断中 | 无多年度 PIT + final-OOS 普适证据前不晋级 |

### 2.1 Phase A–B 摘要（平台与门禁）

- 行情/推荐/监控/回测工具链已可用；生产路径绑定 `primary_50_return_15_drawdown` 证据门槛。
- 历史“达标切片”已被判定为脆弱（如 `auto-iter-032` 2023 反证），**不再作为晋级依据**。
- 用户决策：维持 **50%/15%**，要求**普适化**（大池 + 简化规则）。

### 2.2 Phase C 摘要（当前大池）

历史交接（`HANDOFF.md` / `PLAN.md`）路径：

1. universe → 2. risk → 3. market data → 4. history summary → 5. coverage audit → 6. publish  

大池研究与 **Factor V3 权威链** 目标一致：Jiaoch-only、排除 BSE/STAR、development_only 边界。具体 market 日进度以当时 store 为准；**正式训练物化以 Factor V3 链（Phase D）为当前优先验收口径**。

### 2.3 Phase D 细化计划（Factor V3 物化前置）

| 子阶段 | 目标 | 状态 |
|--------|------|------|
| D0 | 研究目标契约 + readiness 审计脚手架 | ✅ |
| D1 | Jiaoch 连通 / HTTP 网关 / NaN 规范化 | ✅ |
| D2 | Universe 重生 + daily-basic **733** exact-set 封板 | ✅ |
| D3 | feature-history **250** 预窗权威（history-only） | ✅ |
| D4 | materializer v2 **development 注册夹具** + disposable dry-run | ✅ |
| D5 | activation **disposable 证明链** dry-run + formal entry fail-closed | ✅ **当前阶段已验收** |
| D6 | parent-source / factor-v2 evaluation **真实权威 inventory** | ⏳ 下一阶段 |
| D7 | formal activation（`FORMAL_FROZEN_POINTS_CONTRACT`） | ⏳ |
| D8 | materializer v2 **durable native TCB / broker** 审计发布 | ⏳ |
| D9 | formal development materialization 试跑（真实输入） | ⏳ |

### 2.4 Phase E–G（后续，未开始）

- **E**：在 formal 物化数据集上跑 development 训练矩阵；仍 development_only。  
- **F**：独立 embargo / final-OOS；只有双目标 + 稳定性门槛通过才讨论 profile。  
- **G**：生产推荐 profile 注册；网站最多 3 条建议；**永不自动下单**。

---

## 3. 当前进度（2026-08-10）

### 3.1 阶段目标（进行中 / 本会话已验收）

**ID**：`factor-v3-activation-disposable-proof/v1`

**验收内容**：

1. disposable activation：**publish + independent differential verify** 通过  
2. **public formal** `publish_factor_v3_formal_development_input_activation` **拒绝** disposable，且不写 activation-output  
3. 全程 `formal_materialization_eligible=False`、无 production profile、无自动交易  
4. readiness 将 materializer / activation dry-run 指针纳入阻塞检查  

**结果**：通过（见 §4 证据与 §5 readiness）。

### 3.2 数据与权威链状态

| 工件 | 路径 / 标识 | 状态 | 备注 |
|------|-------------|------|------|
| daily-basic 733 | `data/research_runs/audited_pit_factor_v3_daily_basic_collection_v2_development_733_http_publish` | ✅ `status=verified`，733/733 | Jiaoch HTTP daily_basic；NaN→null；exact-set 封板 |
| feature-history 250 | `.../audited_pit_factor_v3_feature_history_collection_v1_development_prewindow_250` | ✅ 回执 verified | `VERIFIED_FEATURE_HISTORY_ONLY`；`factor_materialization_eligible=False`（设计如此） |
| FH 发布 | 同上 `collection-publication` | ✅ `DURABLE_POSTVERIFIED_AND_RETURNED` | 孤儿 `.partial` 已清理 |
| FH 冻结证明 | `data/research_artifacts/factor_v3_feature_history_frozen_source_attestation_v6_http` | ✅ 存在 | 供后续激活绑定 |
| Universe | current_pool / audited universe 重生 | ✅ | 排除 BSE/STAR；支持 exact-set |
| Materializer 开发夹具 | `app/factor_v3_materializer_v2_development_registration_fixture.py` | ✅ surface ready | 仅 `VPS_RUNTIME_ROLE=local_research` 可装；可逆；不写 durable TCB |
| Materializer dry-run | `.../factor_v3_materializer_v2_development_dry_run/LATEST.json` | ✅ `ok=true` | formal produce/run 仍 RED（预期） |
| Activation dry-run | `.../factor_v3_activation_development_dry_run/LATEST.json` | ✅ `ok=true` | disposable 证明；formal entry 拒绝 |
| Compound / formal TCB | `REGISTERED_*=None` | 🔒 fail-closed | 正式路径未发布（预期） |

### 3.3 工程修复要点（本轮相关）

| 提交 | 说明 |
|------|------|
| `beed015` | Jiaoch 网关钉到文档化 **HTTP** 端点（TLS 证书过期场景） |
| `9f826cf` | daily_basic **NaN → null** |
| `be722e3` | daily_basic 保持 HTTP；历史 verify 恢复 HTTPS |
| `c41e881` | universe 重生 + daily-basic 733 exact-set 封板 |
| `ca03f54` | Factor V3 training readiness 审计 |
| `7c6109f` | FH history-only 审计修正 + materializer 开发注册夹具 |
| `3673593` | materializer v2 development dry-run |
| `3cf9f22` | activation disposable 证明阶段 + dry-run + readiness |

### 3.4 关键设计澄清

- **Feature-history 层**回执必须是 history-only：  
  `factor_materialization_eligible=False`（不是 bug）。  
- **`formal_materialization_eligible=True`** 只应由 formal development-input activation 在 **FORMAL** scope 授予，不在 FH 层。  
- **Disposable dry-run ≠ 正式物化**：证明合同引擎与 fail-closed 边界，不替代 native TCB / 真实 parent-source 权威。

---

## 4. 证据索引（本地）

### 4.1 Materializer development dry-run

- 指针：`data/research_runs/factor_v3_materializer_v2_development_dry_run/LATEST.json`  
- `evidence_sha256`：`ed2dac56d73b8b251b2657f08f5f74546570d1a766f19197a085a63e60645fe2`  
- 脚本：`scripts/run_factor_v3_materializer_v2_development_dry_run.py`

```powershell
$env:VPS_RUNTIME_ROLE = "local_research"
.\.venv\Scripts\python.exe scripts\run_factor_v3_materializer_v2_development_dry_run.py
```

### 4.2 Activation disposable dry-run

- 指针：`data/research_runs/factor_v3_activation_development_dry_run/LATEST.json`  
- `stage_goal_id`：`factor-v3-activation-disposable-proof/v1`  
- `evidence_sha256`：`43d50b9bf522d805182f3eb5ed51222a1a67705e9b52628a6bb0d16bc5ab1347`  
- 脚本：`scripts/run_factor_v3_activation_development_dry_run.py`

```powershell
$env:VPS_RUNTIME_ROLE = "local_research"
.\.venv\Scripts\python.exe scripts\run_factor_v3_activation_development_dry_run.py
```

### 4.3 Readiness 审计

- 模块：`app/factor_v3_training_readiness_audit.py`  
- CLI：`scripts/audit_factor_v3_training_readiness.py`  
- 最近一次（2026-08-10）：**Blocking gaps: none**；阶段目标为 activation disposable proof  

```powershell
.\.venv\Scripts\python.exe -m scripts.audit_factor_v3_training_readiness
```

> 注：`data/research_artifacts/*` 与多数 run 产物在 `.gitignore` 中；以本机路径与 git 中的脚本/契约为准。

---

## 5. Readiness 检查清单（当前阶段）

| 检查 ID | 阻塞？ | 最近状态 |
|---------|--------|----------|
| research_goal / 50-15 / 排除 BSE·STAR / 禁自动交易 | 是 | ✅ |
| compound production registry 关闭 | 否 | ✅ |
| materializer durable 槽位关闭 | 否 | ✅（预期未注册） |
| materializer development 夹具 surface | 是 | ✅ |
| materializer calendar 250/483/733 | 是 | ✅ |
| jiaoch 凭据可用 | 是 | ✅（.env） |
| daily-basic 733 verified | 是 | ✅ |
| feature-history 250 + history-only 契约 | 是 | ✅ |
| feature-history 无 .partial | 是 | ✅ |
| materializer development dry-run LATEST ok | 是 | ✅ |
| activation development dry-run LATEST ok | 是 | ✅ |
| materializer formal TCB 已注册 | 否 | ❌ 预期未做 |

字段 `ready_for_formal_development_materialization` 在审计报告中表示：**当前阶段门控清空**，**不是**已获得 `formal_materialization_eligible=True`。

---

## 6. 下一步计划（建议顺序）

### 下一阶段（建议）：`factor-v3-parent-eval-real-authority-inventory/v1`

1. 盘点并绑定 **parent-source** 与 **factor-v2 evaluation** 真实权威路径/哈希  
2. 与 daily-basic 733、FH 250、attestation v6_http **交叉绑定缺口**列表化  
3. readiness 增加「真实权威 inventory 完备」检查  
4. **仍不**打开 formal eligibility / 生产 profile / 自动交易  

### 再往后

5. formal activation（FORMAL scope）— 需真实权威与独立 verifier TCB  
6. materializer v2 durable native TCB / broker **two-commit** 审计发布  
7. formal development materialization（真实输入）+ 证据落盘  
8. development 训练矩阵（仍不碰 embargo/final-OOS）  
9. 仅当 50%/15% + 稳定性达标：讨论 final-OOS 与 profile  

### 明确不做

- 在未通过 formal 与 OOS 前注册生产 profile  
- 自动下单  
- 用 disposable dry-run 冒充 formal 物化完成  
- 把北证/科创板纳入下游可交易宇宙  

---

## 7. 安全与运行边界

| 边界 | 状态 |
|------|------|
| `AUTOMATIC_TRADING_ALLOWED` | `False` |
| `VPS_RUNTIME_ROLE=local_research` | 研究 dry-run 必填 |
| production / recommendation_only | 禁止跑 formal 研究写路径 |
| Embargo / Final-OOS | 未打开、未消费 |
| Token | 仅 `.env` / 环境变量；不进 git |

---

## 8. 关键路径速查

| 用途 | 路径 |
|------|------|
| 研究目标 | `app/research_goal_contract.py` |
| Readiness | `app/factor_v3_training_readiness_audit.py` |
| Materializer 开发夹具 | `app/factor_v3_materializer_v2_development_registration_fixture.py` |
| Materializer 权威槽位（fail-closed） | `app/audited_pit_factor_v3_formal_materializer_v2_authority.py` |
| Activation | `app/factor_v3_formal_development_input_activation.py` |
| 历史交接 | `HANDOFF.md`、`PLAN.md` |
| 本进度文件 | `项目进度.md` |

---

## 9. 进度一览（一张表）

| 层级 | 状态 |
|------|------|
| 平台 MVP / 生产推荐门禁 | ✅ / 🔒 阻断推荐晋级 |
| 50%/15% 普适策略证明 | ❌ 未达成 |
| daily-basic 733 权威 | ✅ |
| feature-history 250 权威（history-only） | ✅ |
| materializer disposable + 开发夹具 | ✅ |
| activation disposable 证明 | ✅ |
| parent / evaluation 真实权威 | ⏳ |
| formal activation | ⏳ |
| formal materialization（真实） | ⏳ |
| development 训练矩阵 | ⏳ |
| final-OOS / 生产 profile | 🚫 |

**当前主线一句话**：物化前置的 **disposable 证明层**已打通；下一刀是 **真实 parent-source / evaluation 权威 inventory**，再谈 formal activation 与 durable TCB。

---

*更新本文件时请同步：阶段 ID、readiness 结果、LATEST.json 哈希、最新 commit、以及「明确不做」是否被违反。*
