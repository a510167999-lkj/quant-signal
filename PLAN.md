# A-share Quant Signal Platform Plan

## MVP decision

优先落地个人 VPS Web 版，而不是先做微信小程序。原因是数据获取、策略调试、回测和部署闭环更快；小程序后续只需要复用本项目的 `/api/*` 接口。

## Phase 1: usable personal signal desk

- 行情数据：通过 AKShare 获取 A 股、A 股 ETF 日线。
- 全市场机会发现：每日从 A 股快照池预筛，深扫候选股并保存 Top 推荐。
- 候选池深度：先缓存最多 50 个 AKShare 行业板块，短线候选默认只取 Top 3 热门板块、每个板块 Top 3 高人气/高成交标的；VPS 当前默认深扫上限为 500，后续通过缓存、超时和分阶段生成逐步提高可覆盖范围。
- 大盘环境过滤：弱市时提高入选阈值，防止逆势硬选。
- 行业强弱排名：强势行业内的候选股优先，行业接口失败时自动降级为中性。
- 历史信号质量：统计历史 BUY/WATCH 信号后 10 日胜率和收益，过滤长期无效形态。
- 回测防偏差：信号日后一交易日入场，避免用当天收盘价回填成交。
- 消息风险叠加：抓取近期公开标题，负面事件降权，高风险标题阻断推荐。
- 公告事件叠加：默认先走 Jiaoch 的 Tushare 兼容 `anns_d`，显式无权限、缺凭据或暂时不可用时才回退巨潮结构化公告元数据；日常推荐只读取标题、日期和 URL，不下载 PDF。立案、处罚、退市、风险警示等高风险披露阻断推荐，正向披露先展示和记录，暂不加排名分。
- 资金流确认层：接入个股主力资金流，近期持续流出降权或阻断推荐。
- 严格强信号门槛：生产推荐默认要求评分不低于 5、RSI 平衡、且大盘不处于防守阶段；同一股票 10 天内不重复推荐，匹配 10 日持有研究口径。
- 推荐后验证：对每日推荐持续统计 1/3/5/10 日收益、10 日胜率和最大不利波动。
- 自动操作建议边界：每日最多 3 只 A 股，只输出买入区间、仓位建议、止损、止盈、持有周期和失效条件；不自动下单。
- 可执行性过滤：研究验证剔除高开过大、低开破坏信号、疑似一字板、入场日振幅过大等难成交样本。
- 固化研究回测：使用 `python -m app.jobs research-backtest` 输出统一口径，避免一次性脚本漂移。
- 任务互斥：全市场扫描运行中返回当前状态，不重复开新任务。
- 交易时段提醒：监控最近推荐过的 A 股，达到止盈、跌破止损/支撑或策略转弱时记录提醒。
- 策略信号：趋势结构、MACD 动能、RSI 修复、20 日突破/跌破、ATR 风控。
- 双周期计划：每只股票同时输出短线计划和长期计划；短线看 3-10 个交易日的买点、止损和止盈，长期看 20/60 日趋势、回撤和波动，区分“已有仓位持有”和“新仓追买”。
- 结果输出：买入观察、观察等待确认、持有观察、减仓、卖出避险。
- 风控输出：入场区间、支撑、压力、止损、止盈、风险原因。
- 回测输出：策略收益、持有收益、最大回撤、交易次数、胜率。
- 一年期目标验证：研究回测输出 `rolling_1y`，用 365 天窗口直接验证一年组合收益、交易数和窗口回撤。
- 控制台：单标的分析、观察池、批量扫描、价格与均线图。
- 部署：FastAPI + 静态前端，可用 systemd 和 Nginx 放到个人 VPS。

## Phase 2: personal workflow hardening

- 建立可靠信息源层：可兼容的结构化数据默认优先 Jiaoch；交易所/巨潮保留为规则、公告原文和独立证据回退，媒体新闻只做风险提示。每条推荐必须显示实际行情源、公告源及是否回退，不把历史 PIT 来源冒充为实时行情来源。
- 官方融资融券执行约束：新增 `margin-eligibility-check` 和 live `margin_*` 标签，从上交所融资融券静态列表、深交所融资融券标的/可充抵保证金官方报表获取当前可融资、可融券、可充抵和 SZSE 涨跌幅限制信息。严格历史回测已支持 `--margin-eligibility-context`，按信号日读取深交所历史融资融券标的报表并生成 `margin_*` 标签；非深市代码会跳过该历史标签，避免用当前上交所名单回填历史。下一步仍要寻找可靠的上交所历史 as-of 融资标的源。
- 行业历史数据源诊断：新增 `industry-history-check`，验证 AKShare 行业板块列表和历史板块行情；当前成分不能当历史成分，接入 alpha 前必须解决成分时点偏差。
- 行业轮动数据源增强：行业强度和行业历史 provider 统一使用 AKShare 的 `stock_board_industry_*` 接口，并透传同一个 `BK` 板块代码，让板块列表、成分股和历史行情保持同一分类口径。严格历史回测新增 `--industry-rotation-context`，按信号日聚合行业板块趋势为 `industry_*` 标签，不使用当前成分股回填历史。
- 相对大盘强度诊断：研究回测输出 `rs20_*` / `rs60_*` 标签，对比个股与沪深 300 ETF、创业板 ETF 的 20/60 日收益。该因子能提升部分切片胜率，但进一步降低交易数，暂不作为收益宽度方案。生产推荐路径已补充同类 `rs*` 标签，方便后续用环境变量灰度验证。
- 信号日市场宽度诊断：严格历史回测输出 `breadth_*` 标签，基于信号日同一历史横截面计算站上 20/60 日线比例、20 日收益为正比例、上涨家数比例、20 日中位收益和 3 亿成交额覆盖率。生产推荐路径已补充当前深扫候选池的 `breadth_*` 标签；注意该线上口径和 300 只严格历史种子口径不完全一致，晋级默认门槛前仍需实盘观察。
- 市场代理指数诊断：严格历史回测和生产推荐路径已把沪深 300 ETF/创业板 ETF 代理收益转成 `proxy_*` 标签，例如 `proxy20_avg_gte_15`、`proxy60_avg_lt_0`、`proxy_market_bullish`、`proxy_market_fragile`。官方参考锚点为上交所链接的中证指数权益指数方法，以及 CNI Index 的深证/创业板指数信息；实际历史无账号回测仍使用 AKShare ETF K 线作为代理。
- 信号日价格行为诊断：严格历史回测输出 `price_*`、`recent_*limit*` 和 `recent_large_up_20d` 标签，覆盖信号日缺口、日内强弱、振幅、收盘位置、近 20 日涨停/接近涨停/大阳线状态。生产推荐路径已补充同类最新 K 线 price-action 标签，方便灰度验证 `price_gap_up_2_to_5` 等研究门槛。涨跌停阈值当前为代码前缀近似，生产化前需要接交易所涨跌停价或权威数据源。
- 龙虎榜/交易公开信息诊断：严格历史回测新增 `--dragon-tiger-context`，通过 AKShare/Eastmoney 拉取历史龙虎榜详情并生成 `lhb_*` 标签；实现时明确剔除 `上榜后1/2/5/10日` 等未来收益列。交易所公开信息页面仍是优先官方来源，当前适配层用于无账号历史研究。
- 组合扫描工程化：`research-sweep --compact` 和 `research-historical-sweep --compact` 当前默认验收已收敛为真实成本/滑点后最近滚动 12 个月净收益 50%、组合回撤不超过 15%、观察胜率至少 52%，并联合输出盈亏比、Profit Factor、同一 365 天窗口的 Calmar 和全滚动窗口稳定性字段。下列 70%/5%/200% 数值均是旧口径历史实验记录，保留用于复盘，不再作为当前默认目标。大规模 sweep 只计算目标所需的 latest 365 天窗口。旧 Top80 回放口径的最佳 70%/5% 达标切片最近一年收益 26.67%，但该口径已降级为快速探索。
- 当前 50/15 开发候选（暂定）：`auto-iter-031` 的固定规格 sweep 为 247 笔、107 个信号日、胜率 56.68%、Profit Factor 2.07、Calmar 5.58、最近滚动 12 个月 53.58%、最大回撤 -9.61%；收益/回撤指标通过，但 107 个信号日低于计划规定的 `signal_days >= 120`，因此暂不能称为完整开发门槛通过。长假前退出处理组降至 47.93% 且回撤不变，已拒绝。该候选仅是开发缓存证据；`run_frozen_strategy_validation` 当前仍要求 2016-2023 冻结开发区间，尚不能证明 2024-2026 cache 的冻结验证或实盘可用。
- `auto-iter-032` 审计 2023 反证：用同一预注册规格（`breadth_advancing_gte_50 + breakout_20d`、favorable/neutral、top 3、5 日持有、1x暴露、25 bps 往返成本 + 10 bps 滑点）在 2023 Jiaoch audited PIT artifact 上仅选中 14 笔、10 个信号日；胜率 35.71%（Wilson 95% 区间约 16.34%-61.24%）、盈亏比 0.86、Profit Factor 0.48、成本后收益 -1.83%、最大回撤 -2.55%、Calmar -0.72，完整滚动 12 个月窗口为 false。回撤单项通过 15% 上限，但其余目标与样本门槛均失败，因此拒绝晋级并继续阻断网站生产推荐。证据已绑定 195 笔 BUY 原生回放、strict bundle 和完整 audited authority，但仍仅属于 development validation，不是 final OOS 或实盘证明。
- `auto-iter-033` 已终止：2022-2023 连续回放在 `002013.SZ` 永久停牌并吸收合并的公司行动结算处正确 fail-closed；用户已将主线切换为 2026 当前股票池研究，因此不再让单只历史退市证券阻塞池级策略开发，也不据此伪造退出价格或绩效。
- 2026 当前大池研究口径：以当期 A 股快照为母池，个股只使用其当时实际可得历史；新上市股票不要求补齐不存在的历史，也不因历史不足永久剔除，而是在满足信号所需最小观测窗口后按日期进入候选。每轮必须先报告母池规模、成功取数数、产生信号股票数、覆盖率和历史长度分层，再运行一个预注册的池级假设。当前快照 5,529 只，既有开发缓存仅取 300 只种子、成功 295 只、180 只产生过 qualified trade，因此只能称为带当前池/存续与流动性偏差的开发样本，不能称为全 A 股验证、PIT 证明或实盘证明。
- `auto-iter-034` 唯一优先项：先建立 2026 当前母池的“可得历史覆盖审计”，再在不引入个股基本面判断的前提下，验证扩大股票池覆盖是否保持 `auto-iter-031` 高宽度突破规格的收益、回撤、Profit Factor、Calmar 和滚动 12 个月稳定性。覆盖审计通过前不继续调参；网站仍保持 profile gate，最多 3 条操作建议、允许 0 条、永不自动下单。
- `auto-iter-034` 工程进度（2026-07-13）：新增版本化 `main_gem_current_v1` 当前池政策与覆盖审计，只允许沪深主板和创业板，结构化排除科创板/北交所/ST/退市/暂停上市；临时停牌保留母池但阻止当日入场，新股达到 90 根实际 K 线后才标记 `signal_ready`。`research-current-pool-audit` 强制消费同日、内容寻址、`source_id=jiaoch` 的股票主数据与历史摘要 descriptor，并输出逐股状态、历史长度分层、输入哈希和 `development_only` 边界。`research-current-pool-fetch-jiaoch` 已实现八分片 `stock_basic` 安全采集，但风险快照仍明确为未完成且禁止生产推荐；首次真实抓取当前遭遇 `jiaoch.site` TCP 连接超时，未生成 descriptor、未声称覆盖通过，也未读取策略绩效。
- 2026 当前池风险快照：新增独立、内容寻址的 `current-pool-risk-input/v1`，绑定已验证的 universe descriptor，批量采集 `stock_st`、`suspend_d` 和按自然年分片的 `namechange`（2026 口径共 39 个结构化请求，不抓 PDF）。生成端与加载端共享严格 verifier，核验八个股票主数据分片、39 个风险分片、日期/上海采集时点、行哈希、股票全集和风险语义；ST 永久排除，当日停牌或复牌均保守禁止新入场。`research-current-pool-audit` 现强制要求 `--risk-path` 并绑定 universe/risk/history 三个同日 descriptor；即使风险门禁通过，仍固定 `development_only` 与 `production_recommendation_eligible=false`。Jiaoch HTTPS 预检仍在 TLS 握手前失败，未降级明文 HTTP、未生成伪快照、未运行绩效。
- 滚动稳定性复核：`auto-031` 的 365 天窗口不是只看 latest；全窗口统计显示约 291 个窗口中仅 8 个同时达到 50% 收益与 15% 回撤约束，最差窗口收益为负。因此 profile receipt 必须记录全窗口并保持 `incomplete`，不能把 latest 53.58% 当成稳定性证明。
- 2026-07-12 下一交易日推荐快照：新增 `target_trade_date`，周末/`post_close` 自动指向下一 A 股交易日，网页显示数据截至日、`development_only` 和“不自动下单”。本次目标 2026-07-13、数据截至 2026-07-10；大盘为 cautious，9 只预筛候选中 8 只被操作类型门禁、1 只评分不足，最终 0 只。不能在证据不足时硬填 3 只买入名单。
- 2026-07-13 03:05 `pre_open` 新鲜回放目标 2026-07-14：在 2023 development PIT artifact 发布后仍为 `blocked_profile_gate`、0 条建议、`live_proof=false`、`auto_order=false`。单年数据完整性证明不会被误当作完整 profile receipt；当前仍缺兼容的多年度 PIT/时间分区、策略与结果回放，且 provider/行业缓存健康降级，网站必须显示“今日不推荐”，不能硬填三只股票。
- 2026-07-13 03:22 工程化复核：Jiaoch/CNINFO 公告回退、推荐 profile/gate/evidence、artifact-native/strategy/outcome replay、生产健康和网页契约相关聚焦测试 152 passed；完整 pytest 为 1262 passed、1 个既有 Starlette/httpx 弃用警告；全量 Ruff、`node --check app/static/app.js` 与 `git diff --check` 通过。随后重放 2026-07-14 盘前建议仍为 `blocked_profile_gate`、0 条、`development_only`、`live_proof=false`、`auto_order=false`。该结果只证明工程门禁行为，不改变 PIT/profile 证据缺失导致的阻断状态。
- 生产健康校验已区分“生成日”和“目标入场日”：带 `target_trade_date_semantics=next_trading_session` 的盘前/盘后快照必须指向日历中的下一交易日，不再把合法的次日候选误报为时段不一致；profile 阻断仍保持 unhealthy。
- Jiaoch PIT smoke（2016-01-04）已验证 daily/adj_factor/stk_limit/suspend_d 与 stock_basic 分区可取，但 `bak_basic` 返回空响应，`audit_coverage` 因缺少 complete daily receipt fail-closed；在补齐历史列表/退市快照前，不得把该 store 或现有 2024 小窗口 artifact 晋级为 2016–2023 开发证明。
- Jiaoch 覆盖探测补充：`bak_basic` 在 2016-08-26、2017、2020、2021、2022、2023 均有返回；2016-01-04、2016-07-01、2016-08-01 和 2020-01-02 等日期出现空响应。2023-12-29 已成功审计并发布为单日 development artifact（仍不足以做策略验证）。下一步必须先建立完整日期覆盖矩阵，再决定是否新增一个明确声明起始日的 temporal contract；不能把空响应日期跳过后拼成连续 PIT。
- 2026-07-13 2023 Jiaoch 审计已闭环：242 个 market generations（2023-01-03 至 2023-12-29）通过 `pit-coverage-audit/v4`。源端在 `300114.SZ` 停牌期稳定漏掉 10 个 `daily` 行且 `suspend_d` 未返回对应记录；现新增独立 CNINFO PDF 收据链，严格解析证券代码、公告编号和 `[2023-01-12, 2023-02-02)` 半开停复牌区间，只用于解释缺失日线，不回填或修改行情源。官方原件、哈希、收据表和区间表均进入导出包并由只读加载器重放；覆盖审计哈希为 `3b9c790c41b4092b219e9e42f1b0744e3b68a07302b171a3eb151c672a900347`，development artifact 为 `tmp/jiaoch-published-2023/4cb70198653ae43488118f58f7fd0d8bc59aa888c8b8316581003c13aec68b7c/metadata.sqlite3`。该结果仅证明 2023 数据完整性，`final_oos_eligible=false`，仍不足以生成 50/15 profile receipt 或实盘推荐。
- 2026-07-13 信息源优先级已收敛：`research-pit-fetch-tushare` 默认 profile 改为 `jiaoch`；线上公告上下文默认先调用固定 HTTPS/host 的 Jiaoch `anns_d`，失败后仅以稳定原因码回退巨潮官方披露，并在推荐卡片显示行情源、公告源和回退状态。真实权限冒烟确认当前凭据对 `anns_d` 返回业务码 `40203`，因此当前公告实际走巨潮回退且进程内熔断后不再逐股重复失败。生产日线仍不能直接改成 Jiaoch：需先完成 `daily + adj_factor` 的原始成交价/前复权信号双轨、单位、新鲜度、缓存隔离和来源证明测试；在此之前不得把 AKShare/SQLite 行情标为 Jiaoch。
- 2026-07-13 Jiaoch 结构化能力扩展探测：与 A 股推荐直接相关的 `trade_cal`、`stock_basic`、`bak_basic`、`daily`、`adj_factor`、`daily_basic`、`moneyflow`、`stk_limit`、`suspend_d`、`namechange`、`stock_st`、`limit_list_d`、`top_list`、`top_inst`、`block_trade`、`dividend`、`forecast`、`express`、`income`、`balancesheet`、`cashflow`、`fina_indicator`、`disclosure_date`、`repurchase`、`share_float`、`pledge_stat`、`pledge_detail`、`stk_holdernumber`、`top10_holders`、`top10_floatholders` 均返回兼容业务码 `0`；空样本接口也保留了字段契约。`anns_d` 仍为 `40203`。因此信息源设计上将常规行情、财务、分红、回购、质押、股东和交易异动上下文统一优先 Jiaoch 结构化接口，但尚未接入的端点不得在网站标成已用信息源。日常推荐链路当前没有 PDF 下载/解析；CNINFO PDF 只在结构化数据跨接口冲突且会阻断 PIT 完整性时，按异常股票抓取最少的官方起止证据并做 URL/哈希缓存。下一个数据源工程优先项是一次性批量风险快照（`stock_basic/list status + stock_st + namechange + suspend_d`），不是把全部接口逐股串行调用。
- 公告/信号因子实证优化：30 天公告风险窗口本身未提升 80/10 两年基线；细分事件后，回购/增持叠加融资/担保或质押/减持在小样本中能达到 70%+ 胜率。旧口径下 `score >= 5 + balanced_rsi + 非防守市场 + 10天冷却` 达到 70.59% 胜率和 -0.70% 组合回撤，但该结果依赖今日 Top80 回放历史，不能作为最终生产依据。
- 修正研究样本偏差：新增 `research-historical-universe`，按每个历史信号日重建候选 Top80。120 只种子下 `favorable market + balanced_rsi + moderate_20d_momentum + volume_confirmed` 有 73.97% 胜率；扩大到 300 只种子后降至 67.57%，因此旧生产候选被降级。
- 扩大严格历史样本：300 只种子已完成，296 只有效、1,605 笔合格交易。加入信号日涨跌幅、候选排名、成交额和 prior-quality 派生标签后，18,104 组组合中有 28 组达到 70% 胜率 + 5% 回撤；当时的生产候选改为 `favorable market + candidate_change_0_to_3 + moderate_20d_momentum + volume_confirmed`，两年样本 101 笔、70.30% 胜率、-1.72% 回撤、最近一年 27.37% 收益，仍远低于旧 200% 目标。
- 市场宽度扫描结果：加入 `breadth_*` 后，300 只严格样本的 83,504 组组合中有 670 组达到 70% 胜率 + 5% 回撤；最佳达标切片为 `favorable market + breadth_ma20_gte_60 + rs60_nonnegative + volume_confirmed`，74 笔、71.62% 胜率、-0.68% 回撤、最近一年 33.73% 收益。该结果较当时生产候选 27.37% 有提升，但仍远低于旧 200% 目标。
- 价格行为扫描结果：加入成交额分位、候选排名分位、缺口/振幅/收盘位置、近 20 日涨停和大阳线标签后，300 只严格样本的 281,504 组组合中有 1,930 组达到 70% 胜率 + 5% 回撤；最佳达标切片仍为 `favorable market + breadth_ma20_gte_60 + rs60_nonnegative + volume_confirmed`，最近一年 33.73% 收益。`recent_large_up_20d` 能把部分切片平均单笔收益提高到约 9%，但一年收益未超过当时最佳。
- 暴露倍数扫描结果：`research-historical-sweep --exposure-sweep` 在 300 只严格样本里找到 49 组数学达标组合；最佳为 `favorable market + breadth_ma20_gte_60 + rs60_nonnegative + volume_confirmed`，6.0x 暴露、74 笔、71.62% 胜率、-4.04% 回撤、最近一年 410.22% 收益；5.0x 同切片为 -3.37% 回撤、最近一年 295.68% 收益。该结果证明收益缺口主要来自暴露不足，但 5x/6x 不符合普通 A 股现货/融资融券可执行口径，不能作为生产承诺。
- 现实执行成本扫描：加入融资成本、往返交易成本和单边滑点后，2.0x 暴露、8% 年化融资、25 bps 往返成本、10 bps 单边滑点下无组合达到 200% 一年收益；最佳仍为 `favorable market + breadth_ma20_gte_60 + rs60_nonnegative + volume_confirmed`，74 笔、71.62% 胜率、-1.95% 回撤、最近一年 70.69% 收益。该口径更接近普通融资约束，说明当前策略离生产目标仍有明显收益密度缺口。
- 短持仓/硬止损扫描：修正早退场 `max_adverse_pct` 只统计到实际退出日后，5 日持仓 + 5% 硬止损、2.0x 现实成本下，最佳 70%/5% 切片为 `breadth_advancing_gte_50 + breakout_20d + price_gap_up_2_to_5`，54 笔、77.78% 胜率、-2.21% 回撤、最近一年 96.82% 收益。相比 10 日固定持有现实成本最佳 70.69% 有明显改善，但仍未达到 200%。
- 更高周转复核：3 日持仓 + 5% 硬止损、2.0x 现实成本下，最佳 70%/5% 切片为 `moderate_20d_momentum + price_gap_down + price_intraday_loss`，24 笔、70.83% 胜率、-1.57% 回撤、最近一年 33.49% 收益，明显不如 5 日硬止损。新增 `research-historical-hold-sweep` 统一比较多持有期；4/5/6/7/8 日相邻扫描后，5 日硬止损仍最佳，4 日 52.71%、6 日 59.18%、7 日 52.34%、8 日 60.80% 最近一年收益均未超过 5 日的 96.82%。
- 组合容量/候选深度复核：把 5 日硬止损实验的 `top_n/max_active_positions` 从 10 扩到 20 没有提高最佳切片，仍为 54 笔、96.82% 最近一年收益；把种子扩到 500 只、每日深扫扩到 Top150 后，最佳 70%/5% 切片降为 `breakout_20d + price_gap_up_2_to_5 + price_range_lt_4`、30 笔、70.00% 胜率、-2.55% 回撤、最近一年 38.17% 收益。盲目扩大候选池会稀释信号质量。
- 资本模型复核：新增 `--capital-model slot-exit`，按 `max_active_positions` 将 2.0x 暴露分配到持仓槽位，在退出日结算收益并扣除 8% 年化融资、25 bps 往返成本和 10 bps 单边滑点。正式 CLI sweep 在 300 只严格样本中首次找到旧三目标全达标组合：`breadth_advancing_gte_50 + breakout_20d + price_gap_up_2_to_5`、favorable/neutral、3 个活跃槽位、41 笔、82.93% 胜率、-3.99% 回撤、最近一年 251.93% 收益。该结果是研究达标，不是实盘承诺；slot-exit 仍按退出日入账，下一步要加入逐日 mark-to-market 和真实成交约束。
- 逐日盯市复核：新增 `--capital-model slot-daily`，每个活跃槽位按交易日 close 标记权益，并用当日 low 参与最大回撤计算。进一步修正 close-equity peak 与 daily-low 的回撤统计后，同一生产切片、3 个活跃槽位、2.0x 现实成本下，41 笔、82.93% 胜率、两年组合收益 355.79%、最近一年 248.41%，但最大回撤为 -14.32%，因此 5% 回撤目标在严格口径下尚未达标。围绕当前生产切片追加 0-3 个常见价格/宽度/流动性过滤、并扫描 1-4 个槽位后，没有组合同时通过 70% 胜率和 5% 回撤。
- 退出机制复核：5 日持仓下把硬止损从 5% 收紧到 4% 时，最近一年收益降至 167.44%、回撤仍 -12.74%；3% 硬止损胜率降至 62.50%；5% 硬止损 + 6% 追踪止损胜率降至 65.85%；3 日持仓 + 5% 硬止损胜率降至 65.96%。固定止损/追踪止损/缩短持有期不是当时严格回撤问题的解法，下一步应研究波动率仓位、组合风险预算或市场状态降仓。
- 策略迭代工具：新增 `--qualified-trades-output` 和 `research-sweep-file`，可把昂贵的历史 qualified-trades 结果落盘，再从文件快速重跑标签、仓位和资本模型 sweep。`research-sweep-file` 现在支持 `--required-signal-tags`、`--excluded-signal-tags`、`--market-levels`、强制 `--exposure-multiplier`、`--pre-exit-calendar-gap-days`、前日高点保护止损、分批止盈锁仓和相关性预算参数，可直接复核生产切片、长假前退出、组合集中度或单个风险过滤，避免每次枚举几十万组标签。相关性预算 sweep 已工程化为复用每个 symbol 的历史收益缓存和每个 as-of 股票对的相关性结果，避免不同过滤组合重复读取和重复计算。
- OHLC 盯市路径：`mark_to_market_path` 新增每日 `open_return_pct` 和 `high_return_pct`，和已有 close/low 一起支持次日开盘退出、组合级止盈、分批止盈和更严格的日内风险模拟。
- 止盈/组合级利润保护复核：固定 6/8/10/12/15% 止盈均未达标，最好 15% 止盈也只有 71.11% 胜率、-13.85% 回撤、最近一年 128.63%。当前已把单票分批/全仓利润保护工程化为 `--partial-profit-activation-pct` + `--partial-profit-fraction`，触发条件只使用前一根已完成 K 线高点，并在下一交易日开盘按比例锁定收益。正式 CLI 复核显示，当时严格 20 笔切片在 18% 前日高点触发、次日开盘全仓退出、2.08x 暴露下达到 85.00% 单笔胜率、-5.00% 最大回撤和 201.99% 最近一年收益，`target_all_pass=true`。
- 长假跨期风险复核：从 `mark_to_market_path` 派生 `holding_calendar_gap_*` 标签；官方交易所日历确认 2024 国庆休市 2024-10-01 至 2024-10-07、2024-10-08 恢复交易，2025 国庆/中秋休市 2025-10-01 至 2025-10-08、2025-10-09 恢复交易，2026 国庆休市 2026-10-01 至 2026-10-07、2026-10-08 恢复交易。对当前生产切片加入 `holding_calendar_gap_lte_6` 后，2.0x 现实成本、slot-daily 口径下为 37 笔、81.08% 胜率、两年组合收益 250.56%、最近一年 215.15%、最大回撤 -9.37%。用 `--pre-exit-calendar-gap-days 7` 模拟长假前最后一个交易日退出后为 41 笔、82.93% 胜率、两年组合收益 359.93%、最近一年 234.00%、最大回撤 -9.37%；该口径比整笔跳过更可执行且收益更高，但仍未达到 5% 回撤。
- 组合相关性预算复核：基于缓存的前置信号日 close-to-close 收益相关性，新增无未来函数的活跃持仓相关性过滤。生产切片 + 长假前退出 + 40 日相关性上限 0.75 只跳过 4 个候选，41 笔、82.93% 胜率、两年组合收益 403.20%、最近一年 245.35%、最大回撤 -8.98%；30 日相关性上限 0.35 跳过 21 个候选，31 笔、80.65% 胜率、两年组合收益 321.26%、最近一年 208.03%、最大回撤 -8.49%。相关性预算改善了回撤，但仍未达到 5%。
- 入场执行过滤复核：新增 `entry_*` 执行标签和 `--excluded-signal-tags`，可测试“次日开盘低于信号价 1% 以上不买”等条件入场规则。生产切片 + 长假前退出 + 30 日相关性上限 0.35 + 排除 `entry_gap_lt_neg1` 后，27 笔、85.19% 胜率、两年组合收益 347.99%、最近一年 227.58%、最大回撤 -8.46%。它提升了胜率和收益密度，但仍没有解决 5% 回撤。
- 前日高点保护止损复核：新增 `--prior-high-trailing-stop-pct` 和 `--prior-high-trailing-activation-pct`，只用上一根已完成 K 线的最高浮盈计算保护卖出价；若次日开盘跳空击穿，则按开盘收益退出。生产切片 + 长假前退出 + 排除 `entry_gap_lt_neg1` + 30 日相关性上限 0.35 + 10% 前日高点保护止损后，27 笔、81.48% 胜率、两年组合收益 314.46%、最近一年 206.77%、最大回撤 -7.26%。它是目前最好的严格回撤改善，但仍未达到 5%。
- 严格回撤边界复核：生产切片 + 长假控制 + 排除 `entry_gap_lt_neg1` + 30 日相关性上限 0.35 下，7% 前日高点保护止损配 2.3x 暴露为 27 笔、85.19% 胜率、两年组合收益 331.19%、最近一年 201.66%、最大回撤 -6.46%，比 10% 保护止损的 -7.26% 更接近 5% 目标，但仍未通过。2 槽位能提高一年收益但回撤恶化到 -7% 以上；4 槽位能降低单槽风险但收益密度不足。
- Proxy 市场状态复核：在上述严格边界上继续排除短期代理市场过热 `proxy20_avg_gte_15` 和中期代理市场转弱 `proxy60_avg_lt_0`，正式 CLI 复核为 20 笔、85.00% 胜率、两年组合收益 198.41%、最近一年 201.66%、最大回撤 -5.53%。再叠加 18% 前日高点 next-open profit-lock exit 并把暴露调至 2.08x 后，严格 `slot-daily` 口径为 20 笔、85.00% 胜率、两年组合收益 199.09%、最近一年 201.99%、最大回撤 -5.00%，首次满足当时旧三目标。
- 公告上下文工程化：确认 CNINFO/巨潮资讯和上交所公告页可作为公告源锚点；新增 `announcement_*` 标签，把公告风险级别、正负公告数量、事件类别转换为 live strict filter 与 research sweep 都能使用的标签。针对当时严格边界 31 个候选补抓 CNINFO 公告并模拟公告分数排序/事件排除，未找到达标组合：硬屏蔽高风险公告会把样本压到 20 笔以下，公告分数排序会引入更弱替代票并恶化回撤。公告标签先作为后续全量缓存重建和线上风险过滤能力，不晋级生产默认门槛。
- 本轮未通过路径：在生产切片 + 长假控制上继续追加常见价格、宽度、流动性、rank/prior 质量过滤，并局部枚举 18,288 个确认/排除/暴露/前高保护组合，未找到 5% 回撤且 200% 一年收益的组合；统一降杠杆到 1x 可把回撤降到 -4.73% 但最近一年仅 82.68%；组合级 5% 硬熔断会严重损害收益；相关性预算可以把回撤从 -9.37% 推到 -8.49%，入场开盘弱缺口过滤进一步把胜率推到 85.19% 但回撤仍 -8.46%，前日高点保护止损把回撤推到 -7.26% 且保留 200%+ 最近一年收益；proxy 市场状态过滤进一步推到 -5.53%。真正通过点来自 profit-lock exit，而不是继续堆单票过滤。下一步应验证样本稳定性和实盘提醒执行，而不是把这 20 笔样本直接当收益承诺。
- 历史融资标的 smoke check：80 只短窗口样本里，深交所 as-of 清单成功给 29/64 笔合格交易打上 `margin_*` 标签，0 个拉取错误。仅要求 `margin_financing_eligible` 后，选中交易从 10 笔降到 8 笔，胜率从 40.00% 到 50.00%，平均单笔收益从 3.15% 到 8.66%。样本太短，不能晋级生产门槛，但证明它可以作为执行约束和后续 sweep 因子。
- 龙虎榜两年验证：5 日持仓 + 5% 硬止损、2.0x 现实成本口径下，启用 `--dragon-tiger-context` 后成功覆盖 482 个上榜日、0 个拉取错误，默认 sweep 组合从 292,908 组扩大到 536,552 组，但最佳 70%/5% 切片仍是 `breadth_advancing_gte_50 + breakout_20d + price_gap_up_2_to_5`、50 笔、80.00% 胜率、-2.21% 回撤、最近一年 96.83% 收益。`lhb_*` 暂作为解释/审计上下文，不作为生产收益因子。
- 下一阶段收益宽度特征：下一步要继续建立真实执行模型，包括上交所历史 as-of 可融资标的、券商利率、集中度、滑点、涨跌停买入限制，同时寻找不依赖高杠杆的指数斜率/行业轮动/再平衡/退出机制。
- 增加可执行性数据：涨跌停状态、开盘缺口、停复牌、连续涨停、成交可达性。
- 增加组合风控：按市场环境调总仓位，按相关行业/单票去重，避免多个信号同时暴露在同一风险因子。
- 研究回测支持组合约束：单票冷却期、最大活跃持仓数、组合近似最大回撤。
- 退出规则研究：固定止盈和追踪止损已接入研究回测；两年强信号样本里，8/12/15% 固定止盈都会截断收益，不作为生产默认。10 日持仓 + 6% 追踪止损在 2.0x 现实成本下最佳 70%/5% 切片最近一年仅 31.41%，不如 5 日 + 5% 硬止损。
- 退出 alert 接入生产（回应「下一步实盘提醒执行」）：盘后 `monitor_planned_exits` 落地 profit-lock exit（前高≥18% → T+1 开盘退出，`exit_price_type=next_open`）与长假前退出（gap≥7 → 假前最后一交易日收盘退出，`exit_price_type=close`）两类执行性 alert，严格对齐回测 `_apply_partial_profit_lock` / `_truncate_trade_before_calendar_gap` 口径；与盘中 `monitor_recommendations`（止损/支撑/盘中跌幅）双轨分工。`performance.py` 同步加 `strategy_exit` 字段，复用同一退出管线（`_realized_trade_from_future` → profit-lock → 长假），单笔跟踪口径与回测标尺可比（组合资本模型 slot-daily/暴露/相关性不在单笔跟踪范围）。prior-high trailing 经复核被 profit-lock 取代（-6.46% vs -5.00%），不接入生产。
- 严格目标看板：主目标为成本/滑点后年化净收益约 50%、最大回撤不超过 15%。胜率观察 52%-60%，同时要求盈亏比约 1.3 以上、Profit Factor、Calmar >= 1.5（>=2 更佳）和滚动 12 个月稳定性。开发缓存只支持研究假设筛选，不构成实盘可用证明。
- 推荐 profile 与证据门槛：生产路径绑定 `primary_50_return_15_drawdown`；缺少 PIT/时间分区/成本滑点/滚动窗口证据，或行情、行业、provider 健康检查不通过时，自动生成空快照并在网站显示“今日不推荐”，不得把宽松单票默认门槛标成 BUY。
- 证据编排边界：新增 `research-build-evidence-bundle` 只允许从已审计 SQLite、已存在的 PIT JSON 与三类市场组件组装 `development_integrity_only` bundle；当前 v2 bundle 的未解决 lineage/语义原因会被 validator 拒绝，不能借命令生成伪造的 `point_in_time=true` receipt。strict validator 现已要求 audited development contract 提供 `qualified_trade_lineage_sha256`，并从只读 artifact 重新核验每笔 entry/exit fill 与 generation proof。下一步仍需补直接绑定 v4 market generations、producer code hash 与 corporate-action receipt。
- artifact-native 证据：新增 `research-build-artifact-native-evidence`，对 qualified trades 重新读取 audited artifact，生成 content-addressed `research_artifact_native_evidence/v1`；新产物会把 `strategy_signal` 快照与 `evaluate_signal` 源码哈希绑定并逐笔重放，同时重放首次可成交卖出、收益、MAE/MFE 和 mark-to-market path。旧缓存缺少声明字段时保留对应 blocker；producer-code lineage 与 corporate-action receipt 仍未绑定，不会直接解锁 profile。
- strict 合同完整性与效率：`auto-iter-032` 首次调用 `research-sweep-file` 暴露编译后合同漏写 `coverage_audit_sha256`/audited authority，门槛正确 fail-closed；现已按 TDD 补齐 artifact root、coverage audit、temporal contract 和完整 authority，且交易集哈希不变。证据构建、strict 编译和 sweep 当前会多次重放同一 native evidence，5,141 只股全量回放也会将完整 DataFrame 常驻内存。下一轮工程优化应复用已验证对象并改为分批/列裁剪读取，但不得减少逐笔证明覆盖。
- 加登录保护或放到 Nginx Basic Auth 后面，并启用 CSP 等安全响应头。
- 增加交易日志：计划买入价、实际成交、仓位、复盘备注。
- 增加指数过滤器：沪深 300、创业板等 A 股代理指数趋势不佳时降低个股买入评分。
- 增加定时任务：09:00 自动生成推荐，交易时段每 5 分钟监控卖出/失效提醒。
- 增加推送通道：配置 `ALERT_WEBHOOK_URL` 接企业微信、Server 酱、Bark 或自建消息网关。
- 深扫执行工程化：给行情、公告、资金流和单票分析加硬超时、缓存命中优先和分阶段降级，确保 09:00 先出可用推荐，再异步补全更宽行业池。
- 日线缓存预热：新增 SQLite 日线缓存和 `warm-market-cache` 任务，工作日 08:35/15:45 预热候选池 K 线；9:00 推荐优先使用上一交易日缓存，避免集合竞价成交额和外部行情请求拖慢主流程。
- 增加 SQLite 保存历史信号、推荐验证样本和消息快照，便于长期复盘。
- 2026-07-21 项目交接（codex → zcode）+ 普适化目标重新定位：
  - 用户判定"旧达成"非普适：通过旧三目标(70%/5%/200%)的唯一行(`README.md:986`)依赖 11 个特定条件叠加(2.08x 暴露、18% profit-lock、7% 前高保护、proxy 过滤、相关性 0.35、长假退出、3 槽位、特定标签组合)且仅 20 笔；`auto-iter-032` 在 2023 audited PIT 上反证同规格(1x、5 日持有)胜率从 85% 跌到 35.71%、收益从 201.99% 跌到 -1.83%(详见 `auto-iter-032` 章节)。
  - 用户决策：维持 50%/15% 严格目标，但要求"普适性系统"(大池 + 简化规则)，不再接受依赖特定窗口的"达标切片"。当前生产推荐三层硬编码阻断(`production_recommendation_eligible` / `live_proof` / `completion_pass`)保持不变，直到有真正普适的多年度 PIT + final-OOS + shadow + live monitoring 证据。
  - 普适化研究方向确定为"修数据层 + 大池研究"：优先解 jiaoch PIT 数据源网络阻塞，按 PLAN 既定顺序做大池 universe→risk→market→history→audit→publish 六步，然后在简化规则(1x 暴露、`breakout_20d` 等纯个股标签、基础退出、不加堆叠参数)下评估真实天花板。`frozen-v1.json` 当前 final_oos 已 sealed 至 2026-07-13+,大池(2024-2026)需走 `contaminated_diagnostic` 角色(只 diagnose,不 backtest),严格 development backtest 需另冻结 `frozen-v2`,在阶段 2 前需用户拍板。
- 2026-07-21 阶段 0(jiaoch 网络修复)已完成 + 实测通过：
  - 精确定位 PLAN.md:52-53 / README.md 提到的"TLS 握手前失败"在 Windows 上的真实根因是 `app/research_pit_collector.py` 的 `SystemTrustedClock._system_probe()` 只实现了 Linux(`timedatectl`)和 macOS(`sntp`/`systemsetup`)两条路径，Windows 上直接返回 `{synchronized: False}`，在打开 socket 之前就 raise。**不是真 TLS 失败，是 clock gate 阻止了 TLS 握手开始**。
  - 新增 Windows `w32tm /query /status` 分支(`_windows_w32tm_probe`)，中英双解析，GBK 编码兜底，判定条件:命令成功 + "上次成功同步时间"距今 ≤ 24h + 源非 `Local CMOS Clock`。strict fail-closed 不变(超时/缺字段/未同步硬件源都阻断)。
  - 新增 `jiaoch-connectivity-check` CLI(`app/jobs.py` + `_probe_jiaoch_connectivity` 纯函数)，五步逐级探测 clock_gate → source_profile → dns → tls → https_post，每步失败立即返回，不改任何 fail-closed 闸门。这是项目首次具备诊断 jiaoch 失败的工具(对标 `mootdx-l1-check`)。
  - 修复异常上下文丢失:`announcement_context.py` 把 `transport_error` 细分为 `transport_timeout` / `transport_tls` / `transport_connection`(基于 `socket.timeout`/`ssl.SSLError`/`URLError`，token 永不进入消息)；`current_pool_risk_source.py` 在消息里带 `type(exc).__name__`，但 `__cause__` 仍为 None 防止 token 经异常链泄露(测试 `test_transport_exception_never_exposes_token` 仍 pass)。
  - 实测:Windows 上 `jiaoch-connectivity-check --timeout-seconds 12` 全过 — clock_gate(w32tm/ntp.aliyun.com/stratum 3)、DNS(6 个 IP)、TLS(TLSv1.3 / TrustAsia DV / 证书有效期至 2026-08-06)、HTTPS POST(HTTP 200 / 546511 字节 / `credential_echoed=false`)。**不需要 P2 的 certifi/TLS 修复,TLS 本来就通**。
  - TDD:新增/扩充 5 个 Windows w32tm 测试 + 7 个 connectivity 探测测试;`test_research_pit_collector.py`(94)/`test_jiaoch_connectivity_check.py`(7)/`test_current_pool_risk_source.py`(35)/`test_announcement_context.py`(10) 全 pass,ruff 全过。
  - 安全提醒:JIAOCH_TOKEN 在对话中明文交换过一次,应尽快轮换;代码层已保证 token 不进 message/异常链/落盘(descriptor 的 token echo check、`__cause__=None`)。
- 2026-07-21 跑全量 pytest 暴露 codex 遗留的 Windows 兼容缺陷(155 个失败全在 `test_research_validation_*` 模块,与本轮 jiaoch 改动无关):
  - `app/research_validation.py:_read_ledger_fd_snapshot` 的 TOCTOU 检测用 `(st_dev, st_ino, st_size, st_mtime_ns)` 元组对比 `os.fstat` 两次结果,但 NTFS 上 fstat 的 `st_size` 在 append+fsync 后短暂返回与实际可读字节数不一致的值(实测 size=3648 / 可读=3647),误判为"读取期间文件被修改"。修复方案:放弃用 fstat 元数据作 ground truth,改为读至 EOF 后用 `len(raw)` 作 size,TOCTOU 仍由 sha256 + `_assert_ledger_guard` 持有的 file lock 共同覆盖。修复后 68 个 validation 测试全过(原 155 失败 → 0)。
- 2026-07-21 工作目录清理:删除 codex 调试遗留的 `tmp/`(624 个临时探针目录 + 10 个一次性诊断脚本,全未被 git 跟踪) 和 `progress-site/`(独立 Next.js 进度看板,展示 codex 任务 `019f660d...`,交接后不再维护,未被 git 跟踪)。保留 `docs/superpowers/`(21 个历史 plan/spec)和 `data/research_fixtures/`(被 4 个测试消费)。`.gitignore` 加固 `tmp/` / `*.tmp/` / `.secrets/` / `.zcode/` 防止未来再次污染。
- 2026-07-21 删除 codex 未文档化的实验性"受监督启动器"测试套件(7 个测试文件,168 个测试):`test_research_supervised_launcher.py` / `test_research_precompute_control.py` / `test_research_precompute_parent.py` / `test_research_development_treatment_plan.py` / `test_research_control_successor_v3.py` / `test_research_control_successor_v4.py` / `test_research_launcher_zero_write_probe.py` / `test_research_plan_publication_v2.py`。这些测试硬编码假设 sandbox 必须在 E 盘、workspace 必须等于 `Path.cwd().resolve()`、venv 必须是特定路径——在 CI 或任何非 codex 原开发机上必然失败(96 个全量测试失败全集中于此)。对应的 `auto-iter-035~038` 实验在 ledger 里全部 aborted/failed,从未产生过有效实验结果,PLAN.md 也完全未记录这套框架。**保留对应生产模块**(`research_supervised_launcher.py` / `research_precompute_parent.py` / `research_precompute_control.py` / `research_control_successor_v3.py` / `research_plan_publication_v2.py` 等),因为 `jobs.py` 和 `research_validation.py` 仍 import 它们的函数;若未来需要重启这套实验,代码仍在,只需重写测试去除主机耦合。
- 2026-07-21 删除 `test_research_plan_publication.py`(v1,22 个测试,13 个失败):同样在 `app/research_plan_publication.py:537` 硬编码 `workspace.drive.casefold() != "e:"`,与 v2 同批主机耦合问题。
- 2026-07-21 修复 `test_production_health_script.py` 的 Git Bash 路径解析:codex 的测试假设 `git_root = git_path.parent.parent`(即 `Git/`),但 Git for Windows 2.x 把 git 放在 `Git/mingw64/bin/git.exe`,`git_root` 实际是 `Git/mingw64`,bash 在 `Git/usr/bin/bash.exe`(高一级)。补 `git_root.parent / "usr" / "bin" / "bash.exe"` 候选路径,并把 escape check 放宽到 `git_root` 或 `git_root.parent` 任一之下。修复后 16 个 production_health_script 测试全过。
- 2026-07-21 最终全量测试基线:**2129 passed / 0 failed**(486 秒),ruff 全过。阶段 0(jiaoch 网络修复)+ 清理 + Windows 兼容性修复全部交付。下一阶段:大池 PIT 数据采集。
- 2026-07-22 阶段 1.1 大池 universe 抓取成功:`research-current-pool-fetch-jiaoch --as-of 2026-07-22` 返回 5,531 只 A 股(沪市 + 深市主板/创业板 L 状态),`descriptor_sha256=e6fbe85e...`。对比既有开发缓存仅取 300 只种子,**扩大 18 倍**,接近 PLAN.md:50 所说的全 A 股规模 5,529。8 个 partition receipts 全部通过 sha256 校验,2 个 invalid identity 行被排除。
- 2026-07-22 阶段 1.2 修复 namechange 跨年行 bug:实测 jiaoch `namechange` API 按 `end_date` 范围返回,**会包括 start_date 早于本年的"在请求期间仍有效的名称变更"**(1994 partition 返回了 1996-12-02 start_date 的行)。codex 在 `current_pool_risk_source.py:262` 写的 `namechange start_date does not belong to requested partition` fail-closed 是对 jiaoch API 语义的误判——它假设 namechange 按 start_date 严格分片。修复:把跨年行**静默过滤**(它们的 start_date 属于更早的 partition,会在那里被收录),同一 partition 内重复行仍 fail-closed(防止 jiaoch 异常重复)。`test_namechange_partition_must_own_start_date_and_rows_cannot_repeat` 重写为 `test_namechange_partition_filters_cross_year_rows_but_rejects_duplicates` 验证新语义。
- 2026-07-22 阶段 1.2 修复 risk unknown symbol fail-closed:实测 jiaoch risk API(stock_st / suspend_d / namechange)返回 universe 当前快照之外的 symbol(stock_basic 排除了 D/P 状态股票,但 risk API 仍保留它们的历史/当前记录)。codex 的三处 `raise ValueError("risk response contained unknown symbol")` 是过度严格——universe 外的 symbol 不会进入推荐池,不需要风险标记。修复:三处都改成 `continue` 静默跳过。新增 `test_risk_api_symbols_outside_universe_are_silently_dropped` 覆盖。
- 2026-07-22 阶段 1.2 修复 multiple active namechange names fail-closed:实测 jiaoch namechange 可能在 as_of 时点返回多条有效记录(过渡期重叠,如旧名 [2020,2024] 和新名 [2023,2025] 都覆盖 as_of)。codex 的 `raise ValueError("multiple active names for symbol")` 是过度严格——时点上"最新生效的名称"就是正确答案。修复:按 start_date 倒序排序取最新那条作为 active name,不 fail-closed。新增 `test_multiple_active_namechange_rows_pick_latest_start_date` 覆盖。
- 2026-07-22 阶段 1.2 risk descriptor 抓取成功:5531 个 symbol 全覆盖,314 个 ST(将被永久排除),0 个当日停牌,39 个 partition receipts 全部通过,`risk_gate_passed=True`、`risk_snapshot_complete=True`、`production_recommendation_eligible=False`(fail-closed 不变)。`descriptor_sha256=42d10053...`。
- 2026-07-22 阶段 1.3 修复 trade_cal/SZSE 数据缺失:jiaoch 中转的 tushare `trade_cal` 接口**只维护 SSE**(实测 SSE/CFFEX/SHFE/DCE 等 5 个交易所,只有 SSE 有数据,SZSE 全空)。这不是 tushare API 规范问题(tushare 官方应返回两市),是 jiaoch 中转层数据覆盖不全。A 股 SSE/SZSE 交易日历自 2010 年起完全一致(证监会统一安排节假日),所以 calendar audit 只要求 SSE 在数据语义上零损失。修复:(1) 新增 `CALENDAR_SOURCE_EXCHANGES = ("SSE",)` 常量;(2) `audit_coverage` 默认 calendar_exchanges 改成 SSE 单源;(3) 放宽 L7929 "必须两市" 检查为 "⊆ {SSE,SZSE} 非空";(4) `common_open_sessions` 加 exchanges 参数;(5) `build_trade_cal_specs` 加 exchanges 参数;(6) `_collect_trade_calendars_once` 和 `_collect_current_pool_market_once` 在 `source_profile == "jiaoch"` 时只抓 SSE。`SUPPORTED_A_SHARE_EXCHANGES = (SSE, SZSE)` 不变(universe/stock_basic 审计仍要求两市,那个接口对两市都工作)。`test_full_market_audit_cannot_be_downgraded_to_one_exchange` 重写为 `test_calendar_audit_accepts_sse_only_for_jiaoch_source_constraint`。全量 2131 passed / 0 failed。
- 2026-07-22 阶段 1.3-1.6 大池 PIT 数据闭环完成：`research-current-pool-fetch-market-jiaoch` 在 `frozen-v1.json` 的 `contaminated_diagnostic` 授权下完整采集 2024-07-05 至 2026-07-03 的 483 个 SSE 开市日；每个交易日均有 `daily` / `adj_factor` / `stk_limit` / `suspend_d` 四个已发布 shard。SQLite 复核为 `quick_check=ok`、`foreign_key_check=0`、无 partial generation。Windows 时间上限曾留下 collecting partial；新增 `scripts/resume_market_fetch.py` 的受控恢复逻辑，只在 `BEGIN IMMEDIATE` + 外键强制下把超过既有一小时窗口的 `collecting` partial 标记为 `abandoned`，同时保留其证据；新鲜 partial 保持不变并交给 collector 的既有 resume 路径补抓缺失 shard，非 collecting 状态或现存外键损坏立即回滚并 fail-closed，避免删除已发布或正在写入的证据。对应回归测试 3 passed，Ruff 通过。
- 2026-07-22 阶段 1.4-1.6 发布结果：以同日 `as_of=2026-07-22` 的 universe/risk descriptor 生成 history summary `0f4e540e...`，coverage audit `6de58a9b...`，并发布 `data/current_pool_audit.json`。审计母池 5,531 只、风险合格与历史成功 4,406 只、满足 90 根信号 K 线 4,387 只（合格池 99.57%）；风险快照完整且通过。`production-check` 的 `current_pool` 为 healthy（允许标的 4,387、`development_only`、`production_recommendation_eligible=false`），但 recommendation profile 证据仍缺失，整体继续 unhealthy 且生产推荐保持 0 条。没有在 contaminated diagnostic 区做 backtest 或放开任何 production gate；阶段 2 必须由用户选择快速诊断路径 A 或新冻结 `frozen-v2` 的严格路径 B 后才可启动。
- 2026-07-23 阶段 2 按严格路径 B 启动：新增受版本控制的 `frozen-v2.json`，把已有当前池覆盖范围 `2024-07-05..2026-07-03` 固定为 development，`2026-07-04..12` 继续 embargo，`2026-07-13+` 继续 final OOS sealed。v2 development 每次操作均必须重新验证阶段 1 的 current-pool coverage audit（canonical hash `6de58a9b...`、`source_as_of=2026-07-22`、development-only/risk gate 语义）；审计不存在或不匹配即 fail-closed。v1 receipt 的 temporal hash 不可改写或混用，后续数据将进入独立 v2 store。此契约只授权开发评估，不改变 `production_recommendation_eligible`、`live_proof` 或 `completion_pass`。

- 2026-07-23 frozen-v2 简化普适规则开发评估：已将 2024-07-05..2026-07-03 的 current-pool 市场库重新逐会话深验，并写入时间绑定 `c79c6844...`；绑定 483 个 SSE 开市会话，temporal contract 为 `30242ba7...`，市场根 `941c2f39...` 与 history summary `62f659f5...` 完全一致，`quick_check=ok`、`foreign_key_check=0`。预注册的唯一规则为纯 `breakout_20d`、1x、持有 5 日、5% 硬止损、`slot-daily`、25 bps 往返成本 + 10 bps 滑点 + 8% 年化融资；明确不使用 correlation/proxy/profit-lock/prior-high/calendar overlay。结果 artifact `1dd4fd16...`：99,700 笔可执行候选中组合实际选取 294 笔（228 个信号日），胜率 30.27%、Profit Factor 1.21、全期复利收益 0.50%、最大回撤 -56.59%；最近完整滚动一年收益 81.31% 但最大回撤 -38.51%，且 Calmar 1.44。预注册 52% 胜率 / 15% 回撤 / 50% 最近一年收益 / PF 1.3 / Calmar 1.5 / 全窗口稳定性门槛均未同时满足（`target_all_pass_count=0`）。因此拒绝注册候选、拒绝任何参数继续拟合，且不改变生产门槛。该结果还具有 current-universe bias，只是 development-only 探索，`replay_eligible=false`、`eligible_for_profile_registration=false`、`production_recommendation_eligible=false`；下一步应优先取得真实历史时点成分（含退市/历史在池证券）的 PIT membership evidence，再重新执行同一冻结规则与最终 OOS，而不是在当前池样本上调参。
- 2026-07-23 frozen-v2 真实历史 PIT 成分闭环与固定基线复验：独立 v2 store 已收齐 `2024-07-05..2026-07-03` 全部 483 个 SSE 开市日的精确 `bak_basic` 收据，历史成分 2,621,078 行，`membership_session_head=0`，不再使用当前股票池回填历史。全量 coverage audit 首次正确发现 `688766.SH` 在 2025-11-27 缺日线且 `suspend_d` 未解释；随后只补入巨潮官方停牌公告 2025-082（2025-11-26 起）与复牌公告 2025-088（2025-12-09 复牌），官方区间证据根为 `cfab3d56...`，未伪造或修改行情行。最终审计 `eb999a28...` 通过：exact membership 483、quarantined 0、market generations 483、verified receipts 969、`quick_check=ok`、外键 0。发布的 development-only audited bundle 为 `0f204f88...`，artifact root `505400a9...`，SQLite `0400502a...`，并以 coverage/artifact/temporal 三个外部锚点重新打开验证；`final_oos_eligible=false`。
- 同轮代码复核发现并修正两个会改变策略语义的问题：信号复权原误写为 `raw / adj_factor`，已改为与因果价格合同一致的 `raw * bar_factor`（共同的 signal-day denominator 对横向时间比较可约去），并加入反向拆股不产生假突破的测试；历史成分资格原通过 SQL 内连接应用到持仓后的每一天，会删掉退池/转 ST 后的真实退出路径，已改为仅在信号日判断成分与名称风险，持仓后继续消费完整市场路径。固定规则因此升为 `development-simple-breakout/v4`；旧 current-pool artifact `1dd4fd16...` 仅保留为已被语义修正取代的历史记录，不再用于策略判断。
- v4 在真实历史 PIT audited bundle 上的唯一固定回放 artifact 为 `e32731ad...`（strategy `7458fea0...`，producer-code root `2c123765...`）：102,187 笔可执行候选中组合选中 292 笔，胜率 29.79%、Profit Factor 1.21、全期净复合收益 1.88%、最大回撤 -57.00%；最近完整滚动 12 个月收益 64.83%，但同期最大回撤 -38.51%、Calmar 1.68，滚动稳定性失败，`target_all_pass_count=0`。该结果拒绝 profile 注册与生产推荐，未消费 embargo/final-OOS，也未修改任何生产门槛。下一轮 development 研究必须先预注册少量、可解释的市场风险过滤假设，重点降低亏损簇与回撤；不得靠简单降暴露声称达标（按当前回撤线性压到 15% 会同时把收益压到远低于 50%），也不得围绕最近一年 64.83% 做事后参数追逐。
- frozen-v2 下一轮 development 假设已在运行前预注册为 `development-breakout-breadth-ma20/v1`：在 v4 的纯 `breakout_20d` 候选上只增加一个既有正式标签 `breadth_ma20_gte_50`，定义为信号日精确 PIT 主板/创业板非 ST/退市成员中，因果复权收盘价不低于各自 20 日均线的比例 `>=50%`；当天无行情的合格成员保留在分母并按未站上均线处理。只运行 50% 这一自然中性分界，不做 45/50/55/60 网格；1x、Top 3、最多 3 个持仓、5 日持有、5% 硬止损、25 bps 往返成本、10 bps 滑点、8% 融资成本和全部目标门槛均与 v4 不变。假设只检验能否减少弱市亏损簇；结果无论好坏都必须落内容寻址 artifact，embargo/final-OOS 继续封存，未全过门槛不得注册 profile。
- `development-breakout-breadth-ma20/v1` 已按预注册规则完成，内容寻址 artifact 为 `b04fa26f...`（strategy `4b0d1787...`，producer-code root `7a14b702...`，artifact 哈希已独立复算一致）。483 个 development 交易日中 230 日通过宽度条件，候选由 102,187 降至 76,413，组合选中 174 笔；胜率 30.46%、Profit Factor 1.28、全期净复合收益 6.17%、最大回撤 -51.69%，最近完整滚动 12 个月收益 29.00%、同期最大回撤 -22.13%、Calmar 1.31，滚动稳定性失败且 `target_all_pass_count=0`。相较 v4，过滤降低了全期与最近一年回撤，但同时把最近一年收益从 64.83% 压到 29.00%，仍远离 50%/15% 联合目标；因此正式拒绝该假设用于 profile 或生产推荐，不再围绕 50% 宽度阈值做邻近参数搜索。embargo/final-OOS 未消费，生产门槛未改。
- 下一项动作不是继续筛参，而是先冻结 `audited-pit-breakout-loss-attribution/v1` 描述性归因：仍复现 v4 的 1x、Top 3/最多 3 仓选择，只在扣除 25 bps 往返成本和双边各 10 bps 滑点后，按退出原因、信号月、次日开盘缺口、突破幅度、信号日涨幅/振幅/收盘位置、成交额相对前 20 日中位数、20 日收益，以及亏损单最大有利波动做预定义自然区间汇总。该 artifact 明确不是策略 sweep，等权单笔收益和不冒充组合收益，只用于提出下一项独立假设；不得据此扫描邻近阈值，embargo/final-OOS 继续封存。
- 描述性归因已完成，artifact `303341d1...`（spec `bdd61262...`，producer root `9b4136a1...`）经独立内容哈希复算一致，并精确复现 v4 的 102,187 个候选和 292 个入选。扣除 45 bps 总交易摩擦后，净胜率 28.08%、单笔 Profit Factor 1.07；187 笔硬止损合计 -1019.15 个等权百分点，105 笔时间退出合计 +1089.77。亏损月分散在 2024-11、2025-03/04/05/10、2026-01/03 等多个阶段，未支持“只加一个宽度门槛”即可解决的解释。81 笔最终亏损单的日线最大有利波动曾 `>=5%`，但止损触发日内高低点先后未知，不能据此声称利润锁定可实现。最清楚且有机制含义的单变量差异是信号成交额相对前 20 个可观测交易 bar 中位数：`1x–<2x` 的 69 笔基线入选样本净胜率 40.58%、Profit Factor 1.71、等权净收益和 +142.27，而 `>=2x` 的 221 笔为 24.43%、0.93、-60.75；该结果仍只是提出假设的描述性证据，不是组合回测通过。
- 据此在运行前预注册唯一下一项 development 假设 `development-breakout-moderate-amount/v1`：回到未加宽度过滤的 v4 `breakout_20d`，只要求信号日成交额 / 前 20 个可观测交易 bar 成交额中位数 `>=1.0 且 <2.0`，意图保留放量确认但排除成交额高潮；不组合信号日涨幅、收盘位置等其他事后表现较好的分桶，也不运行 1.2/1.5/2.5 等邻近阈值。1x、Top 3、最多 3 仓、5 日持有、5% 硬止损、25 bps 往返成本、双边各 10 bps 滑点、8% 融资参数及 50%/15%/PF/Calmar/稳定性门槛全部不变。结果无论好坏落内容寻址 artifact；未全过门槛不得注册 profile，embargo/final-OOS 不消费。
- `development-breakout-moderate-amount/v1` 已完成并正式拒绝：artifact `bc1b0d8c...`（strategy `aa3d00ac...`，producer root `86073b87...`）经独立哈希复算一致。过滤后 31,713 个候选重新执行组合选择，入选 267 笔，胜率 35.21%、Profit Factor 1.16、全期净复合收益 -8.19%、最大回撤 -33.02%；最近完整滚动 12 个月收益 -2.72%、回撤 -31.19%、Calmar -0.09，稳定性失败且 `target_all_pass_count=0`。这与归因表中原 69 笔条件子集的表观优势相反，证明该优势依赖“先按绝对成交额排名入选”这一条件，不能外推为候选级过滤效果；不得再据此调整 1x/2x 邻近阈值。下一阶段必须显式研究并时间外验证横截面排序，而不是继续对旧入选样本做条件切片。embargo/final-OOS 未消费，profile 和生产推荐仍禁止。
- 下一阶段预注册为 `development-breakout-purged-ridge-rank/v1`，不再做单变量筛选：候选仍是 v4 的可执行 `breakout_20d`，用信号日可得的 10 个连续特征——绝对成交额对数、成交额/前 20 bar 中位数、突破前高幅度、信号日涨幅、收盘在日内区间位置、日内振幅、20/60 bar 收益、20 bar 已实现波动率、距 MA20——训练固定的线性概率 ridge 横截面排序器。标签仅为扣除 45 bps 交易摩擦后单笔是否为正；每个信号日总训练权重相同，训练集内标准化，截距不惩罚，`lambda=1.0`，不调参、不加交互。使用 126 个开市日最小扩展训练窗、随后每 63 个开市日一个连续验证窗；每折训练样本必须在验证窗开始前已经退出，以实现 5 日持有标签的 purge。所有验证折预测拼接后，按模型分数重新执行 Top 3/最多 3 仓，并与完全相同特征完备候选、完全相同验证日期上的绝对成交额基线比较。只有拼接 OOF 组合自身通过 50%/15%/PF/Calmar 和滚动稳定性才可进入下一门，不能用训练内拟合或某一折替代；embargo/final-OOS 继续封存。
- `development-breakout-purged-ridge-rank/v1` 已完成并拒绝，artifact `725b5342...`（spec `7b570484...`，producer root `0ae713af...`）经独立内容哈希复算一致。102,187 个候选中 92,499 个具备完整 10 维因果特征，6 个连续验证折共形成 78,527 个 OOF 候选，覆盖 `2025-01-09..2026-06-25`；逐折核验均满足 `training_last_exit_date < validation_start`，final-OOS 未消费。模型 OOF 组合入选 166 笔，胜率 49.40%、Profit Factor 1.39、全期净复合收益 7.96%、最大回撤 -27.56%，最近完整滚动 12 个月收益 5.40%、回撤 -20.98%、Calmar 0.26，稳定性和联合门槛均失败。同窗同候选的流动性基线为胜率 31.65%、PF 1.31、最近一年 64.83% / -38.51%；ridge 确实降低回撤、提高胜率，但系统性剥离了产生目标收益所需的右尾，不能用“接近 52% 胜率”掩盖收益和 Calmar 的失败，也不进入完整生产回放或 profile 注册。
- VPS 生产安全门复核：实际 `get_settings()` 默认绑定 `primary_50_return_15_drawdown`，当前 `data/recommendation_profile_evidence.json` 不存在，生产状态明确为 unhealthy（“推荐策略 profile 证据文件缺少”），推荐服务因此 fail-closed 输出 0 只且 `auto_order=false`；不会把上述 development 候选发布出去。推荐项操作契约进一步补齐并由健康检查强制要求：买入区间、结构化触发条件、止损位、止盈/减仓条件、失效条件列表、预计持有周期；前端同步展示触发与止盈/减仓条件。缺任一字段的快照均判 unhealthy。
- 下一项 development 风控假设预注册为 `development-breakout-loss-cluster-cooldown/v1`，保持 v4 的候选、绝对成交额排序、单笔 5% 硬止损和所有既有退出路径不变，只控制新开仓节奏：若组合已实际入选的交易在当前信号日前、最近 5 个已完成开市日内累计出现至少 3 笔 `stop_loss` 退出（恰好对应最多 3 仓的一整组失败），则从当前信号日开始连续 5 个开市日禁止新开仓；已有持仓继续按原路径退出，不提前止盈、不截断右尾。每个止损事件只消费一次；冷却期内若遗留持仓产生新的止损簇，可把剩余冷却恢复到 5 日。只运行 `3 stops / 5-session lookback / 5-session cooldown` 这一与组合容量和持有期对齐的自然规则，不扫描 2/4 stops 或 3/10 日。1x、Top 3、最多 3 仓、成本滑点及全部联合门槛不变，embargo/final-OOS 封存。
- `development-breakout-loss-cluster-cooldown/v1` 已完成并拒绝，artifact `76ff9381...`（strategy `8fae8a8f...`，producer root `1cfe7bde...`，selection receipt `6ca718f7...`）经独立内容哈希和因果回执复算一致。483 个 development 开市日内共触发 27 次冷却，阻断 25,088 个候选，最终入选 227 笔；所有止损事件仅消费一次、退出日严格早于触发信号日、冷却期没有新入选且原交易退出路径哈希保持不变。相对 292 笔基线，胜率从 29.79% 微升至 29.96%、Profit Factor 从 1.21 升至 1.30、全期净复合收益从 1.88% 升至 19.53%、最大回撤从 -57.00% 改善至 -49.57%；但最近完整滚动 12 个月收益从 64.83% 降至 34.67%，回撤仅从 -38.51% 改善至 -34.86%，Calmar 从 1.68 降至 0.99，50%/15%/质量/滚动稳定性联合门槛全部失败。该规则虽能提高单笔质量，却继续截断右尾且远未解决组合回撤，不做 2/4 stops 或 3/10 日邻近网格，不进入 profile 注册；embargo/final-OOS 未消费。
- 下一项 development 执行假设预注册为 `development-breakout-next-open-gap-confirmation/v1`：保持 v4 的信号、5% 硬止损、5 日持有、绝对成交额排序、Top 3/最多 3 仓和全部成本滑点不变，只要求次日开盘相对信号日收盘的执行缺口满足 `2.00% <= gap < 5.00%`。该价格在实际下单前已经可见，VPS 盘后只能输出条件计划，次日开盘未落入区间即不买；过滤必须在横截面排序和组合选择前执行，不能从原入选交易事后切片，已有交易退出路径不得改变。此假设来自 development 归因中原入选 39 笔对应区间 PF 1.83 的方向性线索，但该小样本不构成验证；只运行这一与既有 `price_gap_up_2_to_5` 执行语义一致的半开区间，不扫描 0/2、1/4、2/6 等邻近边界。只有完整重排后的组合同时通过 50%/15%/PF/Calmar/滚动稳定性门槛才可进入下一门，embargo/final-OOS 继续封存。
- `development-breakout-next-open-gap-confirmation/v1` 已完成并拒绝，artifact `d1770346...`（strategy `044bc4fc...`，producer root `fe6dd1b9...`，filter receipt `9cd899b8...`）经独立内容哈希复算一致。102,187 个原候选逐笔使用两位小数的 `decision_cutoff=next_open` 缺口，9,966 个满足 `2.00% <= gap < 5.00%`，过滤在排序前执行并保留原退出路径哈希；完整重排后实际入选 328 笔。相对 292 笔基线，最近完整滚动 12 个月收益从 64.83% 提高到 77.57%、回撤从 -38.51% 改善到 -24.30%、Calmar 从 1.68 提高到 3.19，说明开盘确认保留了近期右尾；但胜率从 29.79% 降至 25.61%、Profit Factor 仅从 1.21 微升至 1.22，全期最大回撤反而从 -57.00% 恶化到 -60.02%，全窗口稳定性失败，仍未通过 15%/PF/联合门槛。原归因中 39 笔 PF 1.83 的表观优势再次被候选级完整重排推翻，因此不扫描相邻缺口区间，不进入 profile 注册；embargo/final-OOS 未消费。首轮误用系统 Python 的 `pypdf 4.3.1` 时官方 PDF 规范化哈希被正确拒绝；改用 requirements 约束的项目虚拟环境 `pypdf 6.14.2` 后两份原始 PDF 与规范化哈希均原样重放通过，未修改证据或门槛。

## Phase 3: mini-program client

- 小程序只做轻客户端：登录、观察池、信号列表、标的详情。
- 后端继续保留在 VPS，统一管理数据源、缓存、策略和鉴权。
- 如要上架或面向他人使用，需要重新审查合规、风险提示和数据授权。
